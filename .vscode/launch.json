{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "program": "${workspaceRoot}/aBotUI/bin/www",
            "outFiles": [
                "${workspaceRoot}/out/**/*.js"
            ],
            "cwd": "${workspaceRoot}/aBotUI"
        }
    ]
}